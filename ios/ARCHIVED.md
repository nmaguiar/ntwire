# Archived (temporary)

This native iOS/iPadOS client is paused. The code and its CI workflow
(`.github/workflows/ios.yml`, manual-trigger only) stay in the repository so
this can resume later; nothing here has been deleted.

For status and reasoning, see [docs/IOS.md](../docs/IOS.md). For connecting
an iOS/iPadOS device to `ntwire-server` today, use the official WireGuard app
against ntwire's native WireGuard peer support — see
[docs/NATIVE-WIREGUARD.md](../docs/NATIVE-WIREGUARD.md) and, if the server is
behind `ntwire-relay`, [docs/RELAY.md](../docs/RELAY.md#native-wireguard-udp-endpoints).
