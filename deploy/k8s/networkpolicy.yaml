apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: {name: nwire-server}
spec:
  podSelector: {matchLabels: {app: nwire-server}}
  policyTypes: [Ingress, Egress]
  ingress:
    - ports: [{protocol: TCP, port: 8443}, {protocol: UDP, port: 51820}]
  egress:
    - {}
