listen:
  https: ":8443"
  wireguard: ":51820"
auth:
  authorized_keys_dir: /etc/ntwire/keys
  session_ttl: 15m
network:
  tunnel_cidr: 100.64.0.0/16
  advertised_endpoint: "host.docker.internal:51820"
tunnels:
  - name: example
    target: example:8080
    description: Example service
    virtual_port: 18080
    allow: ["*"]
